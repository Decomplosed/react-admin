import React from 'react';
import {
  List,
  Datagrid,
  TextField,
  DateField,
  EditButton,
  DeleteButton,
} from 'react-admin';

const UserList = () => {
  return <List {...props}></List>;
};

export default UserList;
