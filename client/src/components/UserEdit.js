import React from 'react';
import { Edit, SimpleForm, TextInput } from 'react-admin';

const UserEdit = () => {
  return (
    <Edit>
      <SimpleForm>
        <TextInput />
      </SimpleForm>
    </Edit>
  );
};

export default UserEdit;
